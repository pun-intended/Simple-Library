const { sqlForPartialUpdate } = require("./sql")

describe("sqlForPartialUpdate", function() {
    test("Returns object containing correct string of keys", function() {
        const updateData = {}
        const result = 
    })

    test("Returns object containing all correct values", function() {
        const updateData = {}
    })
    
    test("Converts column names given jsToSql argument", function() {

    })
    test("Rejects empty requests", function() {
        const updateData = {}

    })

} )
