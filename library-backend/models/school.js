"use strict"

const db = require('../db')
// Error handlers
// sql for partial update method

// ----- After completion
class School {

    // What methods does the school need?
}

module.exports = School;